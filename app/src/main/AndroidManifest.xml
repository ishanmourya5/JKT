<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ishan.jkt">
    <service
        android:name=".MyFirebaseMessagingService">
        <intent-filter>
            <action android:name="com.google.firebase.MESSAGING_EVENT"/>
        </intent-filter>
    </service>


    <uses-feature android:name="android.hardware.camera"
                    android:required="true"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true">
        <activity android:name=".activities.MainActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.home"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activities.home_receiver"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activities.edit_profile"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activities.edit_username"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activities.UpdateTicket"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustNothing">
        </activity>
        <activity android:name=".activities.ViewTicketReceiver"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activities.ViewTicket"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activities.NewTicket"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustNothing">
        </activity>
        <activity android:name=".activities.ChatTicketHome"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activities.edit_password"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activities.profile_image"
            android:theme="@style/AppTheme">
        </activity>

    </application>

</manifest>